# Timeout қатесін шешу / Fixing Timeout Error

## ❌ Проблема

```
AxiosError: Таймаут запроса. Проверьте API_BASE: /api
code: 'ECONNABORTED'
```

## 🔍 Себебі

1. **API_BASE дұрыс емес:** `/api` (relative path)
   - Сұраным келеді: `http://localhost:5173/api/dashboard`
   - Болуы керек: `http://85.202.193.138:8087/api/dashboard`

2. **Timeout тым қысқа:** 15 секунд

## ✅ Шешім

### 1. Backend URL дұрыс орнатылды

**src/lib/http.js:**
```javascript
const rawBase = 
  import.meta.env.VITE_QAZAQMIND_SERVICE ||
  import.meta.env.VITE_QM_API_URL ||
  "http://85.202.193.138:8087"; // Default backend URL ✅
```

### 2. Timeout ұзартылды

```javascript
const DEFAULT_TIMEOUT = 30000; // 30 секунд (бұрын: 15000)
```

---

## 🚀 Қалай іске қосу

### Қадам 1: Dev серверді тоқтату

**PowerShell терминалда:**
```powershell
# Ctrl+C басыңыз немесе:
taskkill /F /IM node.exe
```

### Қадам 2: Dev серверді қайта іске қосу

```powershell
npm run dev
```

### Қадам 3: Браузерді толық жаңарту

```
Ctrl + Shift + R (Windows)
Cmd + Shift + R (Mac)
```

---

## 🔧 Environment файлдары (опционалды)

Проект түбірінде `.env.local` жасаңыз:

```env
# Backend API URL
VITE_QAZAQMIND_SERVICE=http://85.202.193.138:8087

# HTTP Timeout (30 секунд)
VITE_HTTP_TIMEOUT_MS=30000

# App Version
VITE_APP_VERSION=1.0.0
```

**Ескерту:** `.env.local` файлын жасағаннан кейін dev серверді міндетті түрде қайта іске қосыңыз!

---

## 📊 Консольда тексеру

Dev сервер іске қосылғаннан кейін браузер консолінде (F12) көруіңіз керек:

```
[HTTP] baseURL = http://85.202.193.138:8087
[HTTP] timeout = 30000
```

Егер көрмесеңіз:
1. Dev серверді тоқтатыңыз (Ctrl+C)
2. `npm run dev` қайта іске қосыңыз
3. Браузерді жаңартыңыз (Ctrl+Shift+R)

---

## 🧪 Тестілеу

### 1. Backend қолжетімділігін тексеру

**PowerShell:**
```powershell
curl http://85.202.193.138:8087/api/dashboard
```

**Күтілетін нәтиже:**
- ✅ 401 Unauthorized (токен жоқ - бұл дұрыс)
- ✅ 200 OK (токенмен)
- ❌ Connection refused (backend жұмыс істемейді)
- ❌ Timeout (желі проблемасы)

### 2. Frontend тестілеу

1. Жүйеге кіріңіз
2. F12 → Network табын ашыңыз
3. Бетті жаңартыңыз
4. `/api/dashboard` сұранысын табыңыз
5. Request URL тексеріңіз:
   - ✅ `http://85.202.193.138:8087/api/dashboard`
   - ❌ `http://localhost:5173/api/dashboard`

---

## ⚠️ Жиі кездесетін қателер

### 1. "API_BASE = /api" көрінеді

**Себебі:** Dev сервер қайта іске қоспағансыз

**Шешім:**
```powershell
# Terminal терминалда
Ctrl+C  # Dev серверді тоқтату
npm run dev  # Қайта іске қосу
```

### 2. Әлі таймаут

**Себебі:** Backend сервер жұмыс істемейді немесе қолжетімсіз

**Тексеру:**
```powershell
ping 85.202.193.138
```

**Шешім:**
- Backend командасымен байланысыңыз
- Backend серверді қайта іске қосу керек
- Firewall/network тексеріңіз

### 3. CORS қатесі

**Себебі:** Backend CORS қолдау көрсетпейді

**Шешім:**
Backend-те CORS қосу керек:
```java
@CrossOrigin(origins = "*")
```

---

## 📝 Checklist

- [ ] `src/lib/http.js` өзгертілді
- [ ] Default URL: `http://85.202.193.138:8087`
- [ ] Timeout: 30000ms
- [ ] Dev сервер тоқтатылды (Ctrl+C)
- [ ] Dev сервер қайта іске қосылды (`npm run dev`)
- [ ] Браузер жаңартылды (Ctrl+Shift+R)
- [ ] Консольде `[HTTP] baseURL =` тексерілді
- [ ] Network табында Request URL дұрыс
- [ ] Backend қолжетімді (ping немесе curl)

---

## 🎯 Күтілетін нәтиже

**Қазір:**
```
✅ [HTTP] baseURL = http://85.202.193.138:8087
✅ [HTTP] timeout = 30000
✅ Request URL: http://85.202.193.138:8087/api/dashboard
✅ Status: 200 OK (немесе 401 токенсіз)
```

**Статистика көрінеді:**
- 📊 IQ, EQ, SQ, PQ метрикалары
- 💪 Күшті/Әлсіз жақтары
- 🤖 AI Кеңестері
- 📈 Графиктер және progress барлар

---

## 🆘 Егер әлі жұмыс істемесе

1. **Backend логтарын қараңыз:**
   - Backend қосылу логтарында `/api/dashboard` сұранысы бар ма?
   - Қандай қате қайтарады?

2. **Postman/curl арқылы тексеріңіз:**
   ```powershell
   curl -H "Authorization: Bearer YOUR_TOKEN" http://85.202.193.138:8087/api/dashboard
   ```

3. **Network тексеріңіз:**
   - Интернет бар ма?
   - VPN қосылған ма?
   - Firewall блоктап тұр ма?

4. **Backend URL-ды тексеріңіз:**
   - Backend сервер іске қосылған ба?
   - Дұрыс порт па? (8087)
   - Дұрыс IP па? (85.202.193.138)

---

## 📞 Байланыс

Backend командасына хабарласыңыз:
- Backend сервер статусы
- `/api/dashboard` эндпоинті қолжетімді ме?
- CORS қосылған ба?
- Логтарда қандай қателер бар?

