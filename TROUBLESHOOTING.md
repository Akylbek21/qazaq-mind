# Troubleshooting - Ақауларды жою

## Проблема: 400 Bad Request при запросе `/api/insight/students`

### Себептері (Причины):

1. **Рұқсат жоқ (Нет прав доступа)** - Эндпоинт `/api/insight/students` тек **TEACHER** рөліне қолжетімді
2. **Авторизация жоқ (Не авторизован)** - Токен жоғалған немесе қате
3. **API қолжетімсіз (API недоступен)** - Сервер жұмыс істемейді

### Шешім (Решение):

#### 1. Рөліңізді тексеріңіз (Проверьте свою роль)

Браузер консолінде (F12 → Console) тексеріңіз:
```javascript
console.log("Role:", localStorage.getItem("qm_role"));
console.log("Username:", localStorage.getItem("qm_username"));
console.log("Token:", localStorage.getItem("qm_token") ? "Бар" : "Жоқ");
```

Егер роль `TEACHER` болмаса:
- Жүйеден шығыңыз (Logout)
- Мұғалім рөлімен қайта кіріңіз

#### 2. AuthDebug компонентін пайдаланыңыз

Teacher Console бетінде оң жақ төменгі бұрышта `🔍 Auth Debug` батырмасын басыңыз.
Ол сіздің авторизация ақпаратыңызды көрсетеді:
- Қолданушы аты
- Рөлі
- Токен бар ма
- Мұғалім ме

#### 3. Токенді жаңартыңыз

Егер токен жоқ немесе қате болса:
```javascript
// Браузер консолінде
localStorage.clear();
location.href = "/login";
```

#### 4. API конфигурациясын тексеріңіз

Сервер қосылымын тексеру үшін:
```javascript
// Браузер консолінде
console.log("API Base:", import.meta.env.VITE_QAZAQMIND_SERVICE);
```

Дұрыс мән: `http://85.202.193.138:8087`

#### 5. Network табын тексеріңіз

1. Браузерде F12 → Network
2. Бетті жаңартыңыз
3. `/api/insight/students` сұранысын табыңыз
4. Response табында сервер қайтарған қатені қараңыз

### Жиі кездесетін қателер (Частые ошибки):

| Статус код | Себебі | Шешім |
|------------|---------|--------|
| 400 Bad Request | Нашар параметрлер немесе токен жоқ | Жүйеге кіріңіз (Оқушы немесе Мұғалім) |
| 401 Unauthorized | Токен жоқ немесе қате | Қайта кіріңіз |
| 403 Forbidden | Рұқсат жоқ | Мұғалім рөлімен кіріңіз |
| 404 Not Found | Эндпоинт табылмады | API_BASE дұрыс екенін тексеріңіз |
| 500 Server Error | Сервер қатесі | Сервер логтарын қараңыз |

### Backend талаптары:

Эндпоинт: `GET /api/insight/students`

**Параметрлер:**
- `page` (integer, optional) - Бет нөмірі (default: 0)
- `size` (integer, optional) - Бет өлшемі (default: 20)

**Заголовки:**
- `Authorization: Bearer <TOKEN>` - JWT токен керек
- `Content-Type: application/json`

**Рұқсаттар:**
- `STUDENT` - өз статистикасын ғана көреді (1 элемент)
- `TEACHER` - барлық оқушылардың статистикасын көреді

### Егер проблема шешілмесе:

1. Сервер логтарын қараңыз
2. Backend дамытушымен байланысыңыз
3. Токенді декодтап қараңыз: https://jwt.io/

## Қосымша мәліметтер

### Frontend компоненттері:

- `UserInsightBlock.jsx` - Студент insights блогы
- `DetailedInsightDashboard.jsx` - Детальды аналитика дашборды
- `AuthDebug.jsx` - Авторизация диагностикасы
- `AuthContext.jsx` - Авторизация контексті

### API клиенттері:

- `src/api/dashboard.js` - Dashboard API
- `src/api/client.js` - HTTP клиент
- `src/lib/http.js` - Axios конфигурациясы

### Environment переменные:

Жобада `.env` файлын жасаңыз:
```env
VITE_QAZAQMIND_SERVICE=http://85.202.193.138:8087
VITE_HTTP_TIMEOUT_MS=15000
VITE_APP_VERSION=1.0.0
```

**Ескерту:** `.env` файлын өзгерткеннен кейін dev серверді қайта іске қосыңыз:
```bash
npm run dev
```

